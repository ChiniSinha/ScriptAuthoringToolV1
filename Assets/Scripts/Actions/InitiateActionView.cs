using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class InitiateActionView : MonoBehaviour
{
    public Button preview;
    // Use this for initialization
    void Start()
    {
        preview.interactable = false;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
